// export class event {
//     activate(bindingContext) { //aurelia know cause activate
//         this.item = bindingContext; // gets the model from the model.bind int he compose 
//     }
// }

// the above is commented out to show some time we don't need the viewmodel 
// the view can inherit the viewmodel of it's parent